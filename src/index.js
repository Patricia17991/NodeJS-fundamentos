const express = require('express');

const app = express();

//para o express funcionar precisamos definir uma porta onde a nosssa aplicação vai estar rodando
app.listen() //startando a nossa aplicação
